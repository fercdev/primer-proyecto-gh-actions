name: Cronjob example

on: 
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *' # cada 2 min

jobs:
  cronjob-schedule:
    runs-on: ubuntu-latest
    
    steps:
      - name: Run Cronjob script
        run: echo "Ejecutando cronjob cada 2 minutos"